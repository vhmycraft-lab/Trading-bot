# Your last proposal did not validate

The schema check rejected it with this error:

```
{{error}}
```

Fix **only** what the error names. Do not change the hypothesis, do not
substitute a different idea, and do not simplify the genome to make validation
easier — a proposal that validates because it stopped saying anything is worse
than one that failed.

Return the corrected JSON object in the same shape as before.
